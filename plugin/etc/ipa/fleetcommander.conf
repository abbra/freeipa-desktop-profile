# A configuration for a 'fleetcommander' context
# Defines a non-interactive mode to make sure FleetCommander code does not have
# to deal with files to supply JSON objects

[global]
interactive = False

